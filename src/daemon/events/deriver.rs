// Compatibility shim: keep original module path exporting the split derivations.

pub use crate::daemon::events::derive_hint::StateDeriver;
pub use crate::daemon::events::derive_signal::LockDeriver;
